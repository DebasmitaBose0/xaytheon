name: Greetings

on: [pull_request_target, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
      - uses: actions/first-interaction@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          issue-message: "Welcome to Xaytheon! Thank you for opening your first issue. Our maintainers will review it shortly."
          pr-message: "Welcome to Xaytheon! Thank you for your first pull request. We appreciate your contribution!"
